# options specified here will apply to all posts in this folder

# prevents posts from re-rendering unless explicitly told to do so (for more, see https://quarto.org/docs/projects/code-execution.html#freeze)
freeze: auto

# disable banner title blocks — using minimal custom styling instead
title-block-banner: false

# thank sam csik, check out more options

comments:
  utterances: 
    repo: jdollman/second-person